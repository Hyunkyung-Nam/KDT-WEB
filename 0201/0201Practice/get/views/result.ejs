<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <title>Document</title>
    </head>
    <body>
        <h4>회원가입 완료</h4>
        <div><span><%= userInfo.name %></span>의 가입을 환영합니다</div>
        <div>성별은 <span><%= userInfo.gender%></span>이며</div>
        <div>
            생년월일은 <span><%= userInfo.year %></span>년 <span><%= userInfo.month %></span>월
            <span><%= userInfo.day %></span>일 입니다
        </div>
        <div>
            관심사는 <% for(let interested of userInfo.interested) { %>
            <span><%= interested %></span>
            <% } %> 입니다
        </div>
    </body>
</html>
